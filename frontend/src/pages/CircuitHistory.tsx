import { Box, Typography } from '@mui/material';

const CircuitHistory = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Circuit History
      </Typography>
      <Typography>
        Coming soon: Explore historical data and records for each circuit
      </Typography>
    </Box>
  );
};

export default CircuitHistory; 